import AddReaderForm from "@/components/management/AddReaderForm";

const AddReader = () => {
  return (
    <section className="flex h-[calc(100dvh-12rem)] justify-center items-center">
      <AddReaderForm />
    </section>
  );
};

export default AddReader;
