version: "3.8"

services:
  db:
    container_name: "db"
    image: "postgres:16.2-alpine"
    restart: always
    environment:
      POSTGRES_PASSWORD: "130703"
      POSTGRES_USER: "root"
    ports:
      - "5432:5432"
    volumes:
      - "db:/var/lib/postgresql/data"

volumes:
  db:
